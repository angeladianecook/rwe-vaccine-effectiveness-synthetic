# Data-contract tests: schema, primary keys, allowed values, non-null rules.
# Asserts the generated tables conform to docs/data_dictionary.md.
#
# Status: SCAFFOLD — placeholder so the suite runs; real assertions TODO.

test_that("data-contract tests are defined (placeholder)", {
  skip("Not yet implemented — see PLAN.md §6")
})
