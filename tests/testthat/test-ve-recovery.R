# VE-recovery test: asserts the estimated vaccine effectiveness recovers the
# ground-truth parameter baked into the generator, within tolerance.
#
# Status: SCAFFOLD — placeholder so the suite runs; real assertions TODO.

test_that("estimated VE recovers ground truth (placeholder)", {
  skip("Not yet implemented — see PLAN.md §6")
})
