# Anomaly-catch test: asserts 05_qc_checks.R actually finds the planted
# cross-source classification anomaly (count within tolerance of ground truth).
# This is what makes the "catch" reproducible rather than a story (PLAN.md §4).
#
# Status: SCAFFOLD — placeholder so the suite runs; real assertions TODO.

test_that("QC step detects the planted anomaly (placeholder)", {
  skip("Not yet implemented — see PLAN.md §4")
})
